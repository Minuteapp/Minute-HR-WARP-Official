
import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

export const TaskDialogHeader = () => {
  return (
    <DialogHeader>
      <DialogTitle className="text-xl font-semibold text-gray-800">Neue Aufgabe erstellen</DialogTitle>
    </DialogHeader>
  );
};
