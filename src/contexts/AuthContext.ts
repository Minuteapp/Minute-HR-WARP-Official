
// This is a compatibility file to ensure older imports continue to work
// It re-exports everything from AuthProvider to maintain backward compatibility

export { AuthProvider, useAuth } from './AuthProvider';
